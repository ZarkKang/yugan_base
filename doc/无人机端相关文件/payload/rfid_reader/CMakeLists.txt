cmake_minimum_required(VERSION 3.0.2)
project(rfid_reader)

find_package(catkin REQUIRED COMPONENTS
  rospy
  std_msgs
  diagnostic_msgs
)

catkin_package()

catkin_install_python(PROGRAMS
  scripts/e720_node.py
  DESTINATION ${CATKIN_PACKAGE_BIN_DESTINATION}
)
